// Demonstrates: variables, string concat, loops

jellyfishing (krabby i = 1; i <= 3; i++) {
  shout("Krabby Patty #" + i + " is ready!")
}


// Simple conditional
krabby hungry = aye
aye aye (hungry) {
  shout("I'm ready! I'm ready! Time to make patties!")
} nay nay {
  shout("Not hungry... which never happens.")
}
